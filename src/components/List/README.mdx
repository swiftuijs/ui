import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as ListStories from './stories'

<Meta title="SwiftUI/List/Readme" />

# List

## Overview

List is a container that presents rows of data arranged in a single column. Lists are commonly used to display collections of items, often with dividers between items.

**SwiftUI Correspondence**: Similar to SwiftUI's `List`.

## Basic Usage

<Canvas of={ListStories.Default} />

## Simple List

<Canvas of={ListStories.SimpleList} />

## With Sections

Organize list items into sections:

<Canvas of={ListStories.WithSections} />

## With Actions

Add interactive elements to list items:

<Canvas of={ListStories.WithActions} />

## Custom Styling

Apply custom styles to List:

<Canvas of={ListStories.CustomStyle} />

## Notes

- List items should be wrapped in containers with appropriate padding
- Use Divider component to separate list items
- Lists work well with Section components for grouping
- List supports all standard React children
- Lists are typically used within StandardPage or ScrollView

## Best Practices

1. **Consistent spacing**: Use consistent padding for list items
2. **Dividers**: Use Divider component between items for visual separation
3. **Sections**: Group related items using Section components
4. **Accessibility**: Ensure list items are accessible and keyboard navigable
5. **Performance**: For very long lists, consider virtualization or pagination

