import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as ScrollBounceBehaviorStories from './ScrollBounceBehavior.stories'

<Meta title="SwiftUI/ScrollBounceBehavior/Readme" />

# ScrollBounceBehavior

## Overview

ScrollBounceBehavior adapts SwiftUI's `scrollBounceBehavior` modifier into an explicit wrapper.

**Status**: adapted

## Usage

<Canvas of={ScrollBounceBehaviorStories.Automatic} />

## Variants

<Canvas of={ScrollBounceBehaviorStories.Behaviors} />
