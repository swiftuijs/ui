import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as ScrollTransitionStories from './ScrollTransition.stories'

<Meta title="SwiftUI/ScrollTransition/Readme" />

# ScrollTransition

## Overview

ScrollTransition adapts SwiftUI's `scrollTransition` modifier into an explicit wrapper for web layouts.

**Status**: adapted

## Usage

<Canvas of={ScrollTransitionStories.Opacity} />

## Variants

<Canvas of={ScrollTransitionStories.Scale} />
