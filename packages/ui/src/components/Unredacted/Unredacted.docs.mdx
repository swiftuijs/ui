import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as UnredactedStories from './Unredacted.stories'

<Meta title="SwiftUI/Unredacted/Readme" />

# Unredacted

## Overview

Unredacted adapts SwiftUI's `unredacted` modifier into an explicit wrapper that opts nested content out of placeholder styling.

**Status**: adapted

## Usage

<Canvas of={UnredactedStories.Default} />
