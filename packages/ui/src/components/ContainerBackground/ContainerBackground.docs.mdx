import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as ContainerBackgroundStories from './ContainerBackground.stories'

<Meta title="SwiftUI/ContainerBackground/Readme" />

# ContainerBackground

## Overview

ContainerBackground adapts SwiftUI's `containerBackground(_:for:)` modifier into an explicit surface wrapper for navigation and grouped content areas.

**Status**: adapted

## Usage

<Canvas of={ContainerBackgroundStories.Automatic} />

## Variants

<Canvas of={ContainerBackgroundStories.NavigationSecondary} />
