import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as LinearGradientStories from './LinearGradient.stories'

<Meta title="SwiftUI/LinearGradient/Readme" />

# LinearGradient

## Overview

LinearGradient fills a view using color stops distributed along a line.

**SwiftUI Correspondence**: Mirrors SwiftUI's `LinearGradient` with `stops`, `startPoint`, and `endPoint`.

## Example

<Canvas of={LinearGradientStories.Default} />
