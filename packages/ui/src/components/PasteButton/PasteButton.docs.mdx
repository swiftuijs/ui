import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as PasteButtonStories from './PasteButton.stories'

<Meta title="SwiftUI/PasteButton/Readme" />

# PasteButton

## Overview

PasteButton reads plain text from the clipboard and forwards it to your app, aligned with SwiftUI's clipboard action model.

## Usage

<Canvas of={PasteButtonStories.Default} />
