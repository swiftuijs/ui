import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as ScenePaddingStories from './ScenePadding.stories'

<Meta title="SwiftUI/ScenePadding/Readme" />

# ScenePadding

## Overview

ScenePadding adapts SwiftUI's `scenePadding` modifier into an explicit layout wrapper for web scenes and grouped screens.

**Status**: adapted

## Usage

<Canvas of={ScenePaddingStories.Default} />

## Variants

<Canvas of={ScenePaddingStories.CompactEdges} />
