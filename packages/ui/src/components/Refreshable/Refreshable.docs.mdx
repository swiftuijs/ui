import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as RefreshableStories from './Refreshable.stories'

<Meta title="SwiftUI/Refreshable/Readme" />

# Refreshable

## Overview

Refreshable adapts SwiftUI's `.refreshable` modifier into an explicit web container with a refresh affordance and busy state.

## Usage

<Canvas of={RefreshableStories.Default} />

## Variants

<Canvas of={RefreshableStories.WithToolbar} />
