import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as UnevenRoundedRectangleStories from './UnevenRoundedRectangle.stories'

<Meta title="SwiftUI/Shape/UnevenRoundedRectangle/Readme" />

# UnevenRoundedRectangle

## Overview

UnevenRoundedRectangle extends the rounded rectangle shape by letting each corner radius vary independently.

**Status**: adapted

**SwiftUI Correspondence**: Adapted from SwiftUI's `UnevenRoundedRectangle`.

## Basic Usage

<Canvas of={UnevenRoundedRectangleStories.Default} />

## Stroke Variant

<Canvas of={UnevenRoundedRectangleStories.WithStroke} />
