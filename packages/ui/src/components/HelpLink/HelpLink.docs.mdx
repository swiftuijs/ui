import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as HelpLinkStories from './HelpLink.stories'

<Meta title="SwiftUI/HelpLink/Readme" />

# HelpLink

## Overview

HelpLink presents a dedicated support affordance aligned with SwiftUI's `HelpLink`, adapted to the web as an anchor.

## Usage

<Canvas of={HelpLinkStories.Default} />

## Variants

<Canvas of={HelpLinkStories.CustomLabel} />
