import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as PresentationCornerRadiusStories from './PresentationCornerRadius.stories'

<Meta title="SwiftUI/PresentationCornerRadius/Readme" />

# PresentationCornerRadius

## Overview

PresentationCornerRadius adapts SwiftUI's `presentationCornerRadius` modifier into an explicit wrapper.

**Status**: adapted

## Usage

<Canvas of={PresentationCornerRadiusStories.Default} />

## Variants

<Canvas of={PresentationCornerRadiusStories.RadiusVariants} />
