import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as AngularGradientStories from './AngularGradient.stories'

<Meta title="SwiftUI/AngularGradient/Readme" />

# AngularGradient

## Overview

AngularGradient sweeps color stops around a center point.

**SwiftUI Correspondence**: Mirrors SwiftUI's `AngularGradient` with `stops`, `center`, `startAngle`, and `endAngle`.

## Example

<Canvas of={AngularGradientStories.Default} />
