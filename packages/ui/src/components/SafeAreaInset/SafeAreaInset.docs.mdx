import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as SafeAreaInsetStories from './SafeAreaInset.stories'

<Meta title="SwiftUI/SafeAreaInset/Readme" />

# SafeAreaInset

## Overview

SafeAreaInset adapts SwiftUI's `.safeAreaInset` modifier into an explicit layout container for the web.

## Usage

<Canvas of={SafeAreaInsetStories.Bottom} />

## Variants

<Canvas of={SafeAreaInsetStories.Top} />
