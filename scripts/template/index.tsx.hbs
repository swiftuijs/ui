import { memo } from 'react'
import type { IBaseComponent } from 'src/types'
import { standardizeProps, prefixClass } from 'src/common'

import './style.scss'

export interface I{{ pascalCase name }}Props extends IBaseComponent {
  // your custom props here
}

export const {{ pascalCase name }} = memo(function {{ pascalCase name }} (props: I{{ pascalCase name }}Props) {
  const { ...cProps } = props

  const {commonProps, restProps, children} = standardizeProps(cProps, {
    className: prefixClass('{{ lowerCase name }}')
  })
  
  return (
    <div {...commonProps} {...restProps}>{children}</div>
  )
})