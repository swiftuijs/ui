---
title: "Ellipse"
description: "Ellipse renders an oval surface inside the supplied frame, aligned with SwiftUI's `Ellipse`."
---

## Overview

Ellipse renders an oval surface inside the supplied frame, aligned with SwiftUI's `Ellipse`.
